/**
 * temporary module
 * we should use a module that it can help to define different configuration files for different environment test,prod,local
 * @type {{AUTH_TOKEN: string, API_URL: string}}
 */
export const config = {
    API_URL: 'https://f3rqrapxrfgxbinryu2s3aurkq.appsync-api.eu-west-1.amazonaws.com/graphql',
    AUTH_TOKEN: 'da2-6gsug3rxuja75nqnom67h3x2vi'
};
